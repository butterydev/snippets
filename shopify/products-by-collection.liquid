{% comment %}
// -- Products by collection --//
{% endcomment %}
{%- if collections['COLLECTION_HANDLE_HERE'].products.size > 0 -%}
  {%- for product in collections['COLLECTION_HANDLE_HERE'].products -%}

    <div>collection item</div>

  {%- endfor -%}
{%- endif -%}